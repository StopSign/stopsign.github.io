Using snap.svg:

element.
  append()
  mousemove()
  mouseout()
  mouseover()
  mousedown()
  mouseup()
  hover(fnIn, fnOut)
  animate({attribute: value}, timeInMs)
  
Snap.
  filter. //fancy stuff to images
    grayscale()
    hueRotate()
    brithness()
    contrast()
    saturate()
    sepia()
    shadow()
    invert()
    blur()
  .rect(x, y, width, height)
  .rect(x, y, width, height, rx, ry)
  .circle(x, y, r)
  .group(element1, element2) /*returns group

group.
  attr({attribute: value})
